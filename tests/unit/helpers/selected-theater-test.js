
import { selectedTheater } from 'flick-pick/helpers/selected-theater';
import { module, test } from 'qunit';

module('Unit | Helper | selected theater');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectedTheater([42]);
  assert.ok(result);
});

